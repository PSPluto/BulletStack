using UnityEngine;  [CreateAssetMenu] public class ConservationModifier : Modifier {     public override void Process(Signal signal)     {         float average = (signal.baseDamage + signal.bulletSpeed) / 2f;         signal.baseDamage = average * signal.modMultiplier;         signal.bulletSpeed = average * signal.modMultiplier;     } }